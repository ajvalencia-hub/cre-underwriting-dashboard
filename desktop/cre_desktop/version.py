"""The app's version: the one place to bump for a release. The PyInstaller
spec writes it into the bundle's Info.plist, and the update check compares
it with the latest GitHub release tag (vMAJOR.MINOR.PATCH)."""

VERSION = "1.0.0"
