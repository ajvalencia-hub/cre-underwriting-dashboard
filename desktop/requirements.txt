# Desktop shell (macOS and Windows). Install into a Python 3.12 venv together
# with the backend's requirements. One file for both OSes: platform-only
# packages carry environment markers.
#   macOS:   /opt/homebrew/bin/python3.12 -m venv desktop/.venv
#            desktop/.venv/bin/pip install -r backend/requirements.txt -r desktop/requirements.txt
#   Windows: py -3.12 -m venv desktop\.venv
#            desktop\.venv\Scripts\python -m pip install -r backend\requirements.txt -r desktop\requirements.txt
pywebview==6.2.1   # native window (WKWebView on macOS, Edge WebView2 on Windows) + native file dialogs
keyring==25.7.0    # optional API keys in the macOS Keychain / Windows Credential Manager
pyinstaller==6.22.3  # build-time only: produces the .app / the Windows app folder

# macOS: AppKit/Foundation for the quit hook (cre_desktop/macos.py); pywebview pulls it in too.
pyobjc-framework-Cocoa; sys_platform == "darwin"

# Windows: pywebview's WinForms + WebView2 backend runs on .NET through pythonnet.
pythonnet==3.1.0; sys_platform == "win32"
clr_loader==0.3.1; sys_platform == "win32"
